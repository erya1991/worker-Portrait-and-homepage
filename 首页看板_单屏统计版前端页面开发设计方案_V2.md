# 建筑产业工人服务平台首页看板前端页面开发设计方案（V2：单屏统计版）

## 文档属性

| 项目 | 内容 |
| --- | --- |
| 文档目的 | 作为首页看板前端开发确认稿，仅定义单屏、只读的数据统计展示 |
| 目标工程 | `D:\chen\codex\gongrenhuaxiang`（React + Vite + Tailwind CSS MVP） |
| 页面使用者 | 承包企业管理员、项目管理员、劳务企业管理员等管理人员 |
| 页面定位 | 企业/项目运营数据总览；不承担待办、审批、处置、复核或跨模块跳转功能 |
| 设计基准 | 桌面端 1440 × 900；最小适配宽度 1280，高度 760 |
| 文档版本 | V2.0，2026-08-04 |

## 1. 确认后的范围

### 1.1 页面目标

首页看板只回答“当前授权范围的人员、考勤、合同、AI 识别和评价数据处于什么水平”。页面用于快速浏览，不要求用户在首页完成业务处理。

统计范围按照当前登录管理人员的数据权限确定：

- 承包企业管理员：查看授权企业及授权项目的汇总数据，可切换项目；
- 项目管理员：默认并固定在授权项目，仅查看本项目数据；
- 劳务企业管理员：查看本企业参与项目及本企业工人的汇总数据；
- 建筑产业工人不使用本管理首页。

### 1.2 明确不做

以下内容从首页移除，不以按钮、链接、待办卡片、抽屉或跳转入口的方式呈现：

- 风险待办、陌生人员待复核、合同待签等任务清单；
- 电子合同发起、签署、催签、变更；
- AI 记录复核、风险处置、考勤修正、人脸库维护；
- 单条事件明细、工人身份详情、照片预览和个人敏感信息；
- 首页内的跨模块跳转、审批和人工处理动作。

“陌生人”“考勤差异”等仅以汇总数据或比例展示，不能带有“待办”“处置”“复核”“高风险”等任务化文案。

### 1.3 数据口径边界

| 数据主题 | 首页统计口径 |
| --- | --- |
| 人员与组织 | 当前授权项目范围内，已实名登记、在场、已退场人员及班组数量 |
| 考勤 | 实名制/闸机考勤为权威，统计应考勤、实际到场、到场率、缺少记录数量及 7 日趋势 |
| AI 识别 | 统计已匹配、陌生人和设备在线率；AI 只作现场辅助核验，不改变考勤统计 |
| 电子合同 | 统计签署覆盖率及签署状态分布；不展示人员级待签清单 |
| 工人评价 | 读取最近成功或部分成功评价批次的等级分布、五维平均分、画像覆盖率及工人库汇总 |

低置信度、无人脸、遮挡、多脸和服务异常等无法可靠判断的技术状态不在首页看板展示，也不得直接转换为“陌生人”或风险人员结论。

## 2. 单屏布局设计

### 2.1 高度控制原则

首页内容区不出现纵向滚动条。以现有系统顶部栏高 56px 为前提，在 1440 × 900 屏幕下，内容区可用高度约 844px；看板自身高度控制在 804px 以内，保留页面外边距。

| 区域 | 建议高度 | 说明 |
| --- | ---: | --- |
| 看板工具栏 | 54px | 标题、项目选择、统计周期、数据更新时间、刷新 |
| 核心指标卡 | 112px | 6 张等宽卡片，同一行展示 |
| 第一行统计区 | 270px（低高度窗口 240px） | 人员与考勤趋势、班组圆环占比、AI 识别统计 |
| 第二行统计区 | 剩余高度 | 合同统计、评价统计、数据说明，三张卡片等高填充 |
| 区域间距和内边距 | ≤ 70px | 统一 12px 或 16px 间距 |

实现时首页根容器应使用 `h-full overflow-hidden`，内部用 CSS Grid 固定为“工具栏 / 指标行 / 统计行 1 / 统计行 2”。内容信息过长时，使用省略、缩短文案或小号辅助文本，不能将页面撑高。

在小于 1280 × 760 的窗口中，不转为移动端长页面，也不启用纵向滚动；页面保持最小桌面宽高，浏览器由用户横向/全屏查看。该约束保证本看板作为管理驾驶舱时始终是一屏展示。

### 2.2 页面线框

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ 首页看板  [项目选择] [今日 / 本月]  数据更新时间：10:30  [刷新]              │
├───────────┬───────────┬───────────┬───────────┬───────────┬─────────────────┤
│ 在场人员   │ 今日到场率 │ 合同签署率 │ AI摄像头匹配率 │ 平均评价分 │ A/B 级工人占比 │
├─────────────────────────────────────┬────────────────────────────────────────┤
│ 人员与考勤统计                        │ AI 识别统计                            │
│ 7 日应考勤/实际到场趋势                │ 已匹配、陌生人比例                      │
│ 在场/退场人员、班组构成、出勤摘要      │ 摄像头在线率、近 7 日识别量趋势          │
├───────────────────────┬─────────────┼────────────────────────────────────────┤
│ 电子合同统计           │ 工人评价统计 │ 数据范围与统计说明                       │
│ 签署状态比例、月度趋势 │ 等级分布、五维均分、画像覆盖率           │ 权限范围、数据截止时间、口径说明          │
└───────────────────────┴─────────────┴────────────────────────────────────────┘
```

## 3. 页面与组件设计

### 3.1 看板工具栏：`DashboardToolbar`

| 项目 | 展示规则 |
| --- | --- |
| 标题 | “首页看板”，副标题“企业与项目数据统计总览” |
| 项目选择 | 企业管理员可切换授权项目；项目管理员展示固定项目名称，不提供切换控件 |
| 统计周期 | 仅提供“今日”和“本月”两个统计口径；默认“今日” |
| 数据更新时间 | 显示本次聚合数据的更新时间；评价区域同时显示评价批次的数据截止时间 |
| 刷新 | 只重新读取当前统计数据；不触发同步、重算、校验或任何业务处理 |

### 3.2 核心指标卡：`OverviewMetricCards`

第一行固定 6 张数据卡，无点击、悬停跳转或操作按钮。

| 指标 | 主数值 | 辅助数据 | 数据含义 |
| --- | --- | --- | --- |
| 在场人员 | 在场人数 | 已实名登记人数、班组数 | 当前有效用工关系的人员规模 |
| 今日到场率 | 实到 ÷ 应到 | 实到人数、缺少记录数 | 仅按权威考勤数据计算 |
| 合同签署率 | 已生效合同人数 ÷ 应签人数 | 已生效、签署中、未完成数量 | 合同覆盖情况，不显示待签人员名单 |
| AI摄像头匹配率 | 已匹配 ÷（已匹配 + 陌生人） | 已匹配、陌生人数量 | 仅反映项目人脸库识别汇总 |
| 平均评价分 | 最近正式批次的项目平均分 | 批次名称、覆盖人数 | 只读批次快照，不在页面重新计算 |
| A/B 级工人占比 | A/B 级人数 ÷ 已形成 A-D 等级评价人数 | A、B、C、D 四个等级人数 | 当前版本暂不展示其他等级或补充状态 |

颜色仅表达数据类别，不表达处理优先级：人员/考勤用蓝绿，合同用靛蓝，AI 用青蓝，评价用紫蓝。

### 3.3 人员与考勤统计：`AttendanceStatisticsPanel`

占第一行统计区约 2/3 宽度，包含：

1. 近 7 日双柱趋势图：浅色柱为应考勤人数，主色柱为实际到场人数；图例固定在底部；
2. 考勤摘要：应考勤、实到、缺少进场记录、缺少出场记录四项只读数量；
3. 班组分布：使用圆形占比图展示各班组在场人数及占比，人数较少的班组可合并为“其他”。

趋势图仅显示数值与日期；不显示可点击点位、人员名单或异常明细。

### 3.4 AI 识别统计：`AiRecognitionStatisticsPanel`

占第一行统计区约 1/3 宽度，使用环形比例图与小型趋势图，不展示照片、人员或设备明细。该区域适当收窄，为班组长名称和分布图例预留更多空间。

| 内容 | 展示形式 | 说明 |
| --- | --- | --- |
| 识别结果分布 | 环形图 + 图例 | 已匹配、陌生人；两类独立统计 |
| AI摄像头匹配率 | 环形中心大数值 | 已匹配数量 ÷（已匹配 + 陌生人数量） |
| 设备在线率 | 小统计卡 | 在线设备数 ÷ 已接入设备数 |
| 近 7 日识别量 | 迷你柱状趋势 | 每日抓拍/识别总量，不展示单条事件 |
| 数据说明 | 固定辅助文案 | “陌生人仅表示已完成当前项目人脸库比对且无匹配，不代表风险。” |

### 3.5 电子合同统计：`ContractStatisticsPanel`

占第二行统计区约 30% 宽度，只显示项目合同覆盖和状态分布：

- 合同签署率仪表或环形图；
- 已生效、签署中、未完成、临近到期四项数量；
- 合同状态横向分布条，直观呈现已生效、签署中和未完成的结构；
- 近 6 个月合同生效数量迷你趋势；
- 当前统计范围内的合同总量和数据更新时间。

不显示人员姓名、企业名称、合同编号、签署失败原因或待签列表。

### 3.6 工人评价统计：`EvaluationStatisticsPanel`

占第二行统计区约 40% 宽度，复用现有评价模型的五维与等级口径：

| 内容 | 展示形式 |
| --- | --- |
| 等级分布 | A/B/C/D 横向比例条 |
| 五维平均分 | 职业资质、履约能力、安全行为、工作效率、信用记录 5 条进度条 |
| 评价覆盖率 | 已形成正式评价人数 ÷ 应评价人数 |
| 工人库摘要 | 在场人才数、可复用人才数、有效证书人数三项小统计 |
| 批次说明 | 最近成功或部分成功批次名称、执行时间、数据截止时间 |

等级分布与五维进度区使用等高网格行分配可用空间，避免评价卡片底部出现大块空白。

批次失败或暂无成功批次时，整个评价区域显示“暂无可展示的正式评价统计”，不能以 0 分、D 级或空白柱形替代。

### 3.7 数据范围与统计说明：`DataScopePanel`

占第二行统计区约 30% 宽度，作为单屏页面的可信数据说明，不展示任务或事件。

| 展示项 | 内容 |
| --- | --- |
| 当前权限范围 | 企业名称、项目名称或“授权项目汇总” |
| 人员/考勤数据 | 数据截止时间、权威来源标识“实名制/闸机” |
| AI 数据 | 数据截止时间、辅助核验标识“摄像头/超脑” |
| 合同与评价数据 | 各自更新时间与最近评价批次 |
| 固定口径 | AI 不改变考勤结果；陌生人不代表风险；评价读取批次快照 |

页面增加人员/考勤、AI、合同和评价四个数据来源标签，作为管理人员快速判断统计口径的辅助信息。

## 4. 交互、状态与权限表现

### 4.1 允许的前端交互

- 切换项目；
- 切换“今日 / 本月”统计周期；
- 点击刷新；
- 悬停图表柱体或比例段时显示数值提示。

除以上四项以外，首页没有按钮、列表点击、详情抽屉、菜单跳转、批量操作或二次确认。

### 4.2 页面状态

| 场景 | 表现 |
| --- | --- |
| 首次加载 | 使用与最终卡片高度一致的骨架屏，不改变单屏网格高度 |
| 刷新中 | 刷新图标转动；保留上一轮统计数值，禁止重复刷新 |
| 某统计区无数据 | 在原卡片位置显示“当前统计范围暂无数据”，不显示 0 分或伪造比例 |
| 部分统计失败 | 仅该卡片显示“暂无法获取统计数据”，其他卡片继续展示；不出现重试任务列表 |
| 无权限 | 展示“暂无当前统计范围查看权限”，不返回其他企业或项目的替代数据 |
| 小屏窗口 | 保持最小桌面画布，不触发首页内容纵向滚动；提示用户扩展浏览器窗口 |

## 5. Mock 数据与 UI 数据契约

首页仍使用一个统一的 `getDashboardOverview(filters)` Mock 方法，真实接口替换时应保持以下 UI 数据结构，不让页面直接请求实名制、摄像头、电子签章或评价服务。

```js
{
  scope: { projectName, permissionScope, period, updatedAt },
  overview: {
    onSiteWorkers, registeredWorkers, teamCount,
    attendanceRate, expectedWorkers, presentWorkers, missingEntry, missingExit,
    contractSignRate, contractsActive, contractsSigning, contractsIncomplete, contractsExpiring,
    aiMatchRate, aiMatched, aiStranger, aiPending, cameraOnline, cameraTotal,
    averageScore, evaluationCovered, gradeDistribution
  },
  attendanceTrend: [],
  teamDistribution: [],
  aiTrend: [],
  contractTrend: [],
  evaluation: { batchName, status, completedAt, dataCutoff, grades, dimensions, talentSummary }
}
```

Mock 至少覆盖三种只读统计情形：

1. 项目视角：单项目的完整统计；
2. 企业视角：多个授权项目汇总后的企业统计；
3. 数据不足视角：评价批次缺失或 AI 数据延迟时，按卡片展示数据不可用状态。

## 6. 已执行代码改动范围

| 文件 | 计划处理 | 说明 |
| --- | --- | --- |
| `src/pages/HomeDashboard.jsx` | 已完成 | 删除风险待办、最新事件、合同待签名单、模块跳转和所有点击处置；放大字号与图表，增加班组圆形占比图，并改为单屏统计 Grid |
| `src/mock/dashboard.js` | 已完成 | 删除首页 AI 待确认字段，使用已匹配/陌生人两类统计、趋势和分布数据 |
| `src/AppFixed.jsx` | 已完成 | 首页激活时，内容区使用 `overflow-hidden`，确保页面不出现纵向滚动；其他既有页面维持原滚动行为 |
| `src/pages/dashboard/*` | 未新增 | 保持首页统计逻辑在单文件内，不新增业务操作组件 |
| `README.md`、`PROJECT_CONTEXT.md` | 已完成 | 以最终确认的“单屏统计、Mock 数据、无待办”边界覆盖当前首页描述 |

## 7. 原型验收标准

| 验收项 | 通过标准 |
| --- | --- |
| 单屏性 | 在 1440 × 900 和 1280 × 760 下，首页内容区没有纵向滚动条，所有统计区均可见 |
| 只读性 | 页面没有待办清单、处置按钮、跳转链接、人员明细、抽屉或业务操作 |
| 数据覆盖 | 人员、考勤、合同、AI、评价、工人库六类统计均有展示 |
| 数据边界 | 考勤统计标明实名制/闸机权威；AI 只展示已匹配和陌生人汇总，陌生人不表示风险；评价只读批次快照 |
| 权限适配 | 企业和项目管理人员只看到授权范围的统计；无权限时不泄露替代项目数据 |
| 内容密度 | 文字、图表与卡片均在固定网格内完整展示；无因名称过长撑高页面的情况 |
| Mock 边界 | 所有统计均为 Mock 或后端统一聚合结果；前端不直连外部系统 |

## 8. 待确认事项

1. 企业管理人员的默认首页是“全部授权项目汇总”还是“最近访问项目”；
2. “本月”统计周期是否同时适用于考勤、合同和 AI 识别；
3. 合同签署率分母采用“应签工人”还是“有效用工关系”；
4. AI摄像头匹配率的后端分母是否固定为“已匹配 + 陌生人”，以及异常技术状态的接口处理方式；
5. 评价平均分的展示范围是所有已评价工人，还是仅当前在场工人。

本版方案已按确认意见完成实现；正式接口接入时仍需由后端提供同口径的项目级聚合数据。
