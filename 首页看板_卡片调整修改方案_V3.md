# 首页看板卡片调整修改方案（V3）

## 1. 文档属性

| 项目 | 内容 |
| --- | --- |
| 文档目的 | 在已确认的首页看板单屏统计版基础上，整理本次 9 类卡片调整意见，作为下一轮前端修改依据 |
| 适用工程 | `D:\chen\codex\gongrenhuaxiang` |
| 当前页面 | `src/pages/HomeDashboard.jsx` |
| 当前数据 | `src/mock/dashboard.js` 中的项目级 Mock 聚合数据 |
| 本阶段状态 | 已确认并完成代码修改 |
| 页面定位 | 管理人员使用的单屏、只读统计首页；不增加待办、处置、审批或明细操作 |

## 2. 本轮统一调整原则

1. 首页只保留项目选择，删除统计周期切换和手动刷新入口；页面加载后的数据以当前项目 Mock 聚合结果为准。
2. 所有统计名称使用业务展示口径，不在页面暴露不再使用的“待补充”“生效”“未完成”等旧标签。
3. 所有趋势图同时展示日期、柱体和具体数值，避免只看柱高而无法读取数量。
4. 删除底部“数据范围与统计说明”卡片后，底部只保留合同统计和工人评价统计，并重新分配宽度。
5. 继续保持固定单屏和 `overflow-hidden`，低高度窗口使用紧凑网格，不引入页面纵向滚动。

## 3. 逐项修改方案

### 3.1 首页工具栏和页面标题

当前右侧包含项目选择、“今日/本月”按钮和刷新按钮，标题下方还有“企业与项目数据统计总览 · 只读统计”。本轮调整为：

| 项目 | 调整后方案 |
| --- | --- |
| 页面标题 | 保留“首页看板” |
| 标题说明 | 删除“企业与项目数据统计总览 · 只读统计” |
| 项目选择 | 保留，继续支持切换当前授权项目 |
| 统计周期 | 删除“今日/本月”按钮及相关状态；首页统一展示当前 Mock 的今日统计 |
| 刷新 | 删除刷新按钮、刷新中状态和刷新提示 |
| 前端状态 | 删除 `period`、`refreshing` 及刷新函数；首页不再主动触发通知 |

删除统计周期后，顶部“到场率”统一显示为“今日到场率”。

### 3.2 在场人员卡片

| 当前内容 | 调整后内容 |
| --- | --- |
| 主数值：在场人员 | 保留 |
| 辅助文字：实名登记 136 人 · 12 个班组 | 删除实名登记人数，只保留“12 个班组” |

Mock 中的实名登记字段可以暂时保留给后续人员管理页面使用，但首页不再读取或展示该字段。

### 3.3 合同签署率顶部指标卡

当前辅助文字为“生效 116 · 签署中 8”。调整为：

```text
签署完成 116 · 签署中 8
```

本轮仅调整首页业务展示标签，合同统计数据仍沿用当前项目聚合结果。

### 3.4 A 级工人占比顶部指标卡

| 项目 | 调整后内容 |
| --- | --- |
| 标题 | “A级工人占比” |
| 主指标 | A 级人数 ÷ A-D 已分级工人总数 |
| 辅助文字 | 删除“A-D 已分级工人占比” |

前端建议根据评价等级数组实时计算 A 级占比，不再使用当前的 `abRate` 字段。当前 Mock 示例值按等级计算：P001 约 21.4%，P002 约 16.4%。

### 3.5 人员与考勤统计卡片

#### 3.5.1 卡片说明文字

删除标题下方“实名制/闸机考勤为权威来源”，卡片只保留“人员与考勤统计”标题。

#### 3.5.2 近 7 日到场趋势

当前图表同时绘制“应考勤”和“实际到场”两组柱体。本轮改为单系列图表：

- 只绘制每日实际到场人数；
- 每根柱体上方显示具体人数；
- 横轴显示日期，例如 `07-29`、`07-30`、`今日`；
- 删除“浅色：应考勤 · 蓝色：实际到场”图例；
- 保留单一主色，柱体高度按实际到场人数比例计算。

Mock 数据建议改为：

```text
attendanceTrend: [{ date, present }]
```

现有 `expected` 字段仍可保留在考勤汇总数据中，用于到场率计算，但不在趋势图中展示。

#### 3.5.3 今日考勤数据

在三个小指标卡片上方增加标题“今日考勤数据”，并调整为三列等宽卡片：

| 指标 | 展示内容 |
| --- | --- |
| 实际到场 | 今日实际到场人数 |
| 缺少进场 | 今日缺少进场记录人数 |
| 缺少出场 | 今日缺少出场记录人数 |

删除“应考勤”小卡片，三张剩余卡片使用更宽的横向布局。

### 3.6 AI 识别统计卡片

#### 3.6.1 环形图图例

环形图下方增加两行颜色图例，明确颜色和数量对应关系：

| 颜色 | 图例 | 数量 |
| --- | --- | ---: |
| 青绿色 | 已匹配 | `overview.ai.matched` |
| 红色 | 陌生人 | `overview.ai.stranger` |

环形图中心继续显示匹配率，匹配率按“已匹配 ÷（已匹配 + 陌生人）”计算。

#### 3.6.2 陌生人 Mock 数据

为保证红色区段和图例具有可识别性，Mock 中的陌生人数提高到可观察数量。建议示例：

- P001：已匹配 364，陌生人 18，匹配率约 95.3%；
- P002：已匹配 218，陌生人 10，匹配率约 95.6%。

最终数值以确认后的演示口径为准。

#### 3.6.3 今日识别数量

右侧小卡片“识别总量”改为“今日识别数量”，不再展示历史累计识别总量。建议直接取 7 日识别趋势中“今日”这一项的数量。

#### 3.6.4 近 7 日识别量

趋势图改为带日期和数值的柱状图：

- 每根柱体上方显示每日识别数量；
- 横轴显示日期；
- 数据结构调整为 `[{ date, value }]`；
- “今日识别数量”与趋势图最后一天使用同一数据源，避免两个数字不一致。

### 3.7 电子合同统计卡片

#### 3.7.1 指标和状态名称

统一修改卡片内的状态名称：

| 当前名称 | 调整后名称 |
| --- | --- |
| 已生效 | 签署完成 |
| 未完成 | 初始 |
| 签署中 | 保留 |
| 临近到期 | 保留 |

上述名称同时应用于 6 个指标小卡片和合同状态横向分布条的图例。

#### 3.7.2 合同趋势图

标题由“近 6 个月合同生效量”改为“近 6 个月完成签署合同数量”。图表调整为：

- 每根柱体上方显示具体数量；
- 横轴显示月份，格式为 `2026-02`、`2026-03`、`2026-04`、`2026-05`、`2026-06`、`2026-07`；
- 数据结构由数值数组调整为 `[{ month, value }]`；
- 趋势数值表示每月完成签署合同数量。

### 3.8 工人评价统计卡片

#### 3.8.1 标题区域

删除标题下方的批次和截止日期文字：

```text
2026 年 7 月工人评价批次 · 数据截止 2026-07-31
```

卡片只保留“工人评价统计”标题。

#### 3.8.2 等级分布

将当前 A/B/C/D 横向比例条改为饼图或环形饼图：

- 只展示 A、B、C、D 四个等级；
- 图中显示各等级占比；
- 图例展示等级名称、颜色和人数；
- 不展示“待补充”或其他第五类状态。

#### 3.8.3 卡片内部布局

- 删除底部“评价覆盖”“在场人才”“有效证书”三个指标卡片；
- 删除评价卡片内的其他 `DataStat` 小卡片；
- 评价卡片内部采用左右布局：左侧为 A-D 等级饼图及图例，右侧为五维平均分进度条；
- 删除底部指标后，五维进度区域使用剩余高度均衡分布，避免底部产生新的空白。

### 3.9 数据范围与统计说明卡片

整个“数据范围与统计说明”卡片删除，包括权限范围、当前项目、统计周期、数据更新时间、数据来源标签和底部说明文字。

对应底部布局调整为：

- 底部只保留“电子合同统计”和“工人评价统计”；
- 桌面端建议使用 5 列网格，合同统计占 2 列，评价统计占 3 列；
- 两张卡片等高撑满底部剩余空间；
- 小屏或低高度窗口继续保持单屏，不出现页面纵向滚动。

## 4. 调整后的整体布局

```text
┌──────────────────────────────────────────────────────────────────────────┐
│ 首页看板                                      [项目选择]                  │
├──────────┬──────────┬──────────┬──────────┬──────────┬──────────┤
│ 在场人员 │ 今日到场率 │ 合同签署率 │ AI摄像头  │ 平均评价分 │ A级工人  │
│          │          │          │ 匹配率    │          │ 占比     │
├──────────────────────────────────────────┬───────────────────────────────┤
│ 人员与考勤统计（约 2/3）                  │ AI识别统计（约 1/3）             │
│ 7日实际到场柱状图 + 今日考勤数据          │ 匹配率环形图 + 双图例             │
│ 在场班组圆环图                             │ 今日识别数量 + 7日识别柱状图     │
├───────────────────────┬──────────────────────────────────────────────────┤
│ 电子合同统计（约 2/5） │ 工人评价统计（约 3/5）                           │
│ 状态分布 + 6月完成签署 │ A-D 饼图 + 五维平均分                           │
└───────────────────────┴──────────────────────────────────────────────────┘
```

## 5. 交互和状态变化

| 项目 | 调整后规则 |
| --- | --- |
| 可用交互 | 仅保留项目选择 |
| 删除交互 | 统计周期切换、刷新、刷新中状态、刷新通知 |
| 数据变化 | 切换项目后同步更新所有卡片和趋势图 |
| 空数据 | 对应卡片显示“当前统计范围暂无数据”，不绘制伪造柱体或饼图 |
| 页面滚动 | 根容器继续使用 `overflow-hidden`，不增加纵向滚动 |

## 6. 已执行代码和文档调整范围

| 文件 | 预计调整内容 |
| --- | --- |
| `src/pages/HomeDashboard.jsx` | 已完成删除周期/刷新状态；调整 6 个顶部指标；重构考勤、AI、合同和评价图表；删除数据范围卡片；重排底部网格 |
| `src/mock/dashboard.js` | 已完成调整考勤、AI 和合同趋势数据结构；增加可识别的陌生人 Mock；评价等级仅保留 A-D；补充月份和每日数值 |
| `src/index.css` | 已完成根据底部两卡片和新图表高度调整单屏网格及低高度窗口规则 |
| `README.md` | 已完成更新首页交互、统计口径和卡片范围说明 |
| `PROJECT_CONTEXT.md` | 已完成记录首页仅保留项目选择、评价仅展示 A-D 等确认边界 |
| `首页看板_单屏统计版前端页面开发设计方案_V2.md` | 保留 V2 基线；本 V3 作为本轮实施补充 |

## 7. 验收标准

1. 首页顶部不存在“今日/本月”按钮、刷新按钮和副标题说明文字，只保留项目选择。
2. 在场人员卡片辅助文字只显示班组数量，不显示实名登记人数。
3. 合同相关区域不再出现“生效”和“未完成”，统一显示“签署完成”和“初始”。
4. 顶部评价指标显示“A级工人占比”，不显示旧的 A-D 占比说明文字。
5. 考勤趋势图只有实际到场单柱，每天柱体上有数量，底部三项考勤指标上方有“今日考勤数据”。
6. AI 环形图下方能清楚看到已匹配和陌生人两种颜色图例；右侧显示“今日识别数量”；7 日柱状图有日期和每日数量。
7. 合同趋势图显示具体数量和 `2026-07` 形式的月份，状态条与指标卡片名称一致。
8. 工人评价卡片没有批次提示、底部三项指标和“待补充”项，使用 A-D 饼图和五维进度条填充布局。
9. 数据范围与统计说明卡片已删除，底部两张卡片等高且不出现明显大块空白。
10. 在 1440 × 900 和 1280 × 760 下首页内容完整可见，不出现纵向滚动条。

## 8. 已确认事项

1. AI 陌生人 Mock 执行 P001 为 18 人、P002 为 10 人；
2. 合同“初始”已替代当前“未完成”状态；
3. 数据范围与统计说明卡片已删除，权限范围和数据更新时间不在首页展示；
4. “今日识别数量”直接取 7 日识别趋势中的“今日”值。
